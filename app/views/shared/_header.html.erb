<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item ">
          <%= link_to "Post", posts_path, :class => "navbar-brand" %>
          <!-- <a class="nav-link active" aria-current="page" href="#">Home</a> -->
        </li>
        <li class="nav-item ">
             <%= link_to "Contact us", contact_path, :class => "navbar-brand"%>
          <!-- <a class="nav-link" href="#">Link</a> -->
        </li>
         <li class="nav-item">
             <%= link_to "About us", about_path, :class => "navbar-brand"%>
          <!-- <a class="nav-link" href="#">Link</a> -->
        </li>
      </ul>
          <form class="d-flex" role="search">
           <% if user_signed_in? %>
       logged in as :<strong><%= current_user.email %></strong>
       <%= button_to "Edit user " ,  edit_user_registration_path , style: 'color:white; background-color:black;', :class => 'navbar-link' %>
      <%= button_to 'Logout', destroy_user_session_path, style: 'color:white; background-color:black;', method: "delete", data: { confirm: 'Are you sure?' } %>
      <%else%>
       <%= link_to " Sign up" ,   new_user_registration_path, style: 'color:white; background-color:black; padding:6px;', :class => 'navbar-brand' %>
       <%= link_to " Sign In" ,   new_user_session_path,  style: 'color:white; background-color:black; padding:6px;',   :class => 'navbar-brand' %>
  <% end %>

      </form>
    </div>
  </div>
</nav>
